## 개요

- 자바스크립트에서 모든 값은 타입을 갖는다
    - 참고) 변수 자체는 값이 아니고 식별자이기 때문에 타입을 갖지 않음
- **자바스크립트의 타입**
  - **기본형 (원시형)** : Number, String, Boolean, null, undefined, Symbol
  - **참조형** : 객체 (배열, 함수, 날짜, RegExp, Map, Set)
    

---

## 6.1 숫자 타입

- **number**
    - 딱 하나의 숫자타입
    - 배정밀도 62비트 부동소수점 형식 ⇒ 모든 수를 실수로 처리
    - `+Infinity`,  `-Infinity`, `NaN`
        - 대소문자를 구분하는 것 주의

---

## 6.2 문자열 타입

- **string**
    - 텍스트 데이터를 나타냄
    - `''`  or `""` or ````
    - 다른 언어와 달리 원시 타입이므로 변경 불가능
- **템플릿 리터럴**
    - ES6 부터 도입
    - 백틱 (`) 을 이용하여 표현
    - 가능해진 것
        - 멀티라인 문자열 가능 : 이스케이프 시퀀스 (`\n`) 없이도!
        - 표현식 삽입 가능 : `${표현식}`

---

## 6.4 불리언 타입

- **boolean**
    - `true` or `false`
    - 조건문에서 자주 사용됨

---

## 6.5 undefined 타입

- **undefined**
    - 값으로 undefined만 존재
    - 자바스크립트 엔진이 맨 처음에 넣어두는 값 ⇒ 즉 한번도 할당이 된 적이 없다는 뜻!

---

## 6.6 null 타입

- **null**
    - 값으로 null만 존재
    - 변수에 값이 없음을 의도적으로 명시하는 것
    - 함수가 유효한 값을 반환할 수 없는 경우 명시적으로 null을 반환하기도

---

## 6.7 심벌 타입

- **Symbol**
    - ES6 에서 추가됨
    - 다른 값과 중복되지 않는 유일무이한 값
    - 객체의 유일한 프로퍼티 키를 만들기 위해 사용
    - `Symbol()` 함수를 호출하여 생성함

---

## 6.8 객체 타입

- 자바스크립트는 객체 기반의 언어 ⇒ 거의 모든 것이 객체다!!

---

## 6.9 타입은 왜 필요할까

- 값을 저장할 때 확보해야 하는 메모리 공간의 크기를 결정하기 위해
- 값을 참조할 때 한번에 읽어 들여야 할 메모리 공간의 크기를 결정하기 위해
- 메모리에서 읽어 들인 2진수를 어떻게 해석할지 결정하기 위해

---

## 6.10 자바스크립트는 동적 타이핑

- **정적 타이핑**
    - Java, C
    - 변수를 선언할 때 할당할 수 있는 데이터 타입을 선언 (명시적 타입 선언)
    - 컴파일 시점에 타입 체크가 가능함
- **동적 타이핑**
    - JavaScript
    - 변수를 선언할 때 타입을 선언하지 않아, 어떠한 값이든 자유롭게 할당 가능
    - 선언이 아닌 할당에 의해 타입이 추론됨
    - 재할당에 의해 언제든지 타입이 변할 수 있음
    - 유연성이 높지만 신뢰성이 떨어짐
- **동적 타이핑일 때 주의할 점**
    - 변수는 꼭 필요할 때만 제한적으로 사용
    - 변수의 스코프를 최대한 좁게
    - 전역 변수는 최대한 쓰지 마
    - 변수 보다는 상수를 쓰자
    - 변수명을 잘 짓자

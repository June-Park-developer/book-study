## 개요

- **제어문**
    - 코드의 실행 흐름을 인위적으로 제어
    - 조건문 + 반복문
    - 가독성에 주의할 것

---

## 8.1 블록문

- **블록문** (= 코드 블록, 블록)
    - 형태 : { 0개 이상의 문 }
    - 하나의 실행단위
    - 끝에 `;` 붙이지 않음 (어차피 종결되는 거 보이잖아)

---

## 8.2 조건문

- **역할**
    - 조건식(결과: 불리언)의 평과 결과에 따라 블록문을 실행할지 말지 결정함

### 8.2.1 if…else 문

- **형태**
    
    ```jsx
    if (조건식1) {
    	// 조건식1이 참일 때 돌릴 코드블록
    } else if (조건식2){
    	// 조건식 1은 거짓이고 조건식 2가 참일 때 돌릴 코드블록
    } else {
    	// 다 거짓일 때 돌릴 코드블록
    }
    ```
    
    - else if, else 는 옵션
    - else if 는 여러번 가능, else 는 딱 한번만

### 8.2.2 switch 문

- **역할**
    - 표현식을 평가하고, 그 값과 일치하는 case 문으로 실행 흐름을 옮김
- **형태**
    
    ```jsx
    switch (표현식) {
    	case 값1: 표현식이 값1일 때 실행할 구문;
    		break;
    	case 값2: 표현식이 값2일 때 실행할 구문;
    		break;
    	case 값3: 표현식이 값3일 때 실행할 구문;
    		break;
    	default: 맞는게 없을 때 실행할 구문;
    }	
    ```
    
    - default 는 선택 사항이며 break 가 필요하지 않음
- **어떨 때 쓰나**
    - if else : 논리적 참, 거짓으로 어떤 코드 블록을 실행할지 결정할 때
    - switch : 논리적 참, 거짓 보다는 다양한 case 에 따라 어떤 코드 블록을 실행할지 결정할 때
        - case의 표현식에는 주로 문자열, 숫자 등의 특정 값이 잘 온다
    - ⇒ if else를 권장하나, 조건이 많은 경우에는 switch가 가독성이 더 좋으니 추천
- **폴스루 (Fall Through)**
    - break 가 없으면 switch 문을 탈출하지 않고 끝까지 다 실행함 (그 이후의 case 들과 default)
    - 여러개의 case문을 하나의 조건으로 사용할 수 있어 유용하기도
    - 

---

## 8.3 반복문

- **역할**
    - 조건식의 평가 결과가 참이면 블록 실행 → 다시 조건식 평가해서 여전히 참이면 또 실행 → 조건식이 거짓일 될 때까지 반복
- **반복문의 대체**
    - forEach 메서드 (배열 순회)
    - for…in문 (객체 프로퍼티 열거)
    - for…of문 (이터러블 순회)

<aside>

### for문 vs while문

- **for문**
    - 반복 횟수가 명확할 때
- **while문**
    - 반복 횟수가 불명확할 때
</aside>

### 8.3.1 for문

- **형태**
    
    ```tsx
    for (변수 선언문; 조건식; 증감식) {
    	// 조건식이 참일 동안 반복할 코드
    }
    ```
    
    - 변수 선언문, 조건식, 증감식은 모두 옵션 ⇒ 선언하지 않으면 무한루프
- **중첩 for 문**
    - for문 안에 for문

### 8.3.2 while 문

- 형태
    
    ```tsx
    while (조건식) {
    	//조건식이 참일 동안 반복할 코드
    }
    ```
    
    - 조건식이 언제나 참이면 (예: true) 무한 루프
        - 탈출 하려면 if 문으로 탈출 조건 만들고 break 사용

### 8.3.3 do…while 문

- while 문은 한번도 실행 안될 수도 있는데, do while문은 한번은 꼭 실행함
- **형태**
    
    ```tsx
    do {
    	// 한번은 무조건 실행하고, 조건식이 참일 동안 반복할 코드
    } while (조건식);
    ```
    

---

## 8.4 break문

- **역할**
    - 레이블 문, 반복문, switch문의 코드 브록을 탈출함
    - 모든 블록을 탈출하는 것이 아니며, 이외에 사용하면 에러남
- **레이블 문**
    - 식별자가 붙은 문
        - 예: `foo: console.log('foo');`
    - break 문에 식별자 붙여서 딱 그 문을 탈출하게 해줌
        - 예: `break foo;`
    - 역할
        - 실행 순서를 제어
        - 중첩된 for문 외부로 탈출하고 싶을 때, 이외에는 권장 X

---

## 8.5 continue문

- **역할**
    - 반복문의 코드 블록 진행을 중단하고 증감식으로 실행흐름을 이동시킬 때
    - 즉, 반복문을 탈출하는ㅇ게 아니라 나머지 블록은 안돌리고 그 다음 반복으로 넘어가는거지
- **언제 사용하나**
    - for 문 내에서 조건이 또 들어갈 때!
        - if 문을 정직하게 쓰기
        - if+continue로 아닐 때는 비켜나가게 쓰기
        
        ```tsx
        // for 문 내에서 특정 조건에서만 뭔가 하고 싶을 때, 
        
        // (1) if 문 사용
        if (조건식) { 
        	// 만족 시 실행할 코드
        } 
        
        // (2) continue 사용
        if (!조건식) continue;
        
        	// 만족 시 실행할 코드
        ```
        
    - 조건 만족 시 실행할 코드가 길다면, continue가 더 간단함

## 9.1 타입 변환이란?

- **타입 변환의 종류**
    - **명시적 타입 변환**
        - = 타입 캐스팅
        - 개발자가 의도적으로 값의 타입을 변환
    - **암묵적 타입 변환**
        - = 타입 강제 변환
        - 개발자의 의도와 상관 없이 JS 엔진에 의해 암묵적으로 변환
- **타입 변환은 기존 변수를 변경하는 것이 아님**
    - 기존 변수를 바꾸는 것이 아니라, 기존 원시값을 이용해 다른 타입의 새로운 원시 값을 생성하는 것!
        
        ```jsx
        let x = 10;
        let str = x.toString();
        
        // str 은 string, "10"
        // x 는 여전히 number, 10
        ```
        
- **중요한 건, 타입 변환을 이해해서 예측할 수 있어야 한다는 것!**

---

## 9.2 암묵적 타입 변환

### 9.2.1 문자열 타입으로

- **경우**
    - `+` 가 문자열 연결 연산자로 동작하는데 문자열이 아닌 값이 있는 경우
    - 템플릿 리터럴에서 표현식 (${`표현식`})
- **생소한 부분**
    - null → “null”
    - undefined → “undefined”
    - Symbol() → 에러남
    - 객체 타입들
        - “[object Object]” 등 신기한 결과가 됨. 잘 쓰이지는 않을 것 같아 옮기지는 않음.

### 9.2.2 숫자 타입으로

- **경우**
    - 산술 연산자에서 숫자 아닌 값이 있는 경우 → 변환 불가하면 NaN
    - 비교 연산자에서 숫자 아닌 값이 있는 경우
    - `+` 단항 연산자
- **생소한 부분**
    - +null → 0
    - +undefined → NaN
    - 객체 타입은 대체로 → NaN 이지만, 빈 배열(`[]`)은 → 0

### 9.2.3 불리언 타입으로

- **경우**
    - 조건식 (조건문, 반복문, 삼항조건 연산자)
- **Falsy한 값**
    - false
    - undefined
    - null
    - 0, -0
    - NaN
    - ‘ ‘ (빈 문자열)
- **이 외에는 모두 Truthy**
- **생소한 부분**
    - ‘0’ → Truthy (빈 문자열이 아니잖아)
    - [ ], { } → Truthy (falsy 아니니까)

---

## 9.3 명시적 타입 변환

- **방법**
    - 표준 빌트인 생성자 함수를 new 없이 호출하기
    - 빌트인 메서드를 사용하기
    - 암묵적 타입 변환을 사용하기

### 9.3.1 문자열 타입으로 변환

- **방법**
    - String 생성자 함수를 new 연산자 없이 호출하기
        
        ```jsx
        String(1);  // "1"
        String(true); // "true"
        ```
        
    - Object.prototype.toString 메서드 사용하기
        
        ```jsx
        (1).toString(); // "1"
        (true).toString; // "true"
        ```
        
    - 문자열 연결 연산자 사용하기
        
        ```jsx
        1 + ''; // "1"
        true + ''; // "true"
        ```
        

### 9.3.2 숫자 타입으로 변환

- **방법**
    - Number 생성자 함수를 new 연산자 없이 호출하기
        
        ```jsx
        Number('0'); // 0
        ```
        
    - parseInt, parseFloat 함수 사용하기 (문자열만 변환 가능)
        
        ```jsx
        parseInt('0'); // 0
        parseFloat('10.53'); // 10.53
        ```
        
    - 단항 산술 연산자 사용하기 (`+`, `*` )
        
        ```jsx
        +'0'; // -> 0
        *'0'; // -> 0
        ```
        

### 9.3.3 불리언 타입으로 변환

- 방법
    - Boolean 생성자 함수를 new 연산자 없이 호출하기
        
        ```jsx
        Boolean(`false`); // -> true
        // Falsy 아니면 다 ture가 된다는 거 기억하라~
        ```
        
    - `!!` 사용하기
        
        ```jsx
        !!0; // -> false
        ```
        

---

## 9.4 논리 연산자의 단축평가

- 표현식을 평가하는 도중에 평가가 확정되면 나머지 평가 과정을 생략해버림
- 논리 연산자는 **언제나 2개의 피연산자 중 어느 한쪽으로 평가됨**
    - 논리 연산자의 결과를 결정하는 피연산자를 (타입 변환 없이) 그대로 반환!
- **if 문을 대체할 수 있음**
    - 어떤 조건이 Truthy 일 때 뭔가를 해야한다면 ⇒ `&&` 사용
    - 어떤 조건이 Falsy 일 때 뭔가를 해야한다면 ⇒ `||` 사용

### 논리합 (`||`)

- **결과**
    - 좌항이 true면 좌항
    - 좌항이 false면 우항
- **쓰임새**
    - **함수 매개변수 또는 변수에 기본값 설정할 때**
        - falsy 하면 대신 이 값 넣어줘! 이니까 논리합 이지
        
        ```jsx
        function getStringLength(str) {
        	str = str || '';  // str 이 Truthy 하면 str을 대입하고, 없으면 빈 문자열을 넣어
        	return str.length; // .length는 문자열에만 쓸 수 있으니까 확실하게
        }
        ```
        
- **관련 사항**
    - ES6 부터는 **매개변수에** **기본값 설정**이 가능해짐
        
        ```jsx
        functiohn getStringLength(str = '') {
        	return str.length;
        }
        ```
        
    - **`??` : null 병합 연산자 (매개변수는 아니고 변수에서만 쓰는 거지만)**
    
    ### `??` null 병합 연산자
    
    - **쉽게 말해, 변수 값 할당에서의`||` 단축 평가의 더 안전 버전**
    - **기능**
        - 좌항이 null 또는 undefined ⇒ 우항을 반환
        - 아니면 ⇒ 좌항을 반환
    - **쓰임새**
        - 변수에 기본값을 설정할 때
            - 바로 위의 `||` 단축평가는 null, undefined 외에 falsy 한 값이 더 있기 때문에 오류 가능성 O
                
                ⇒  `??`로 해결!
                

### **논리곱(`&&`)**

- **결과**
    - 좌항이 true이면 우항
    - 좌항이 false이면 좌항
- **쓰임새**
    - **참조 시 객체를 가리키기를 기대하는 변수가 null 또는 undefined (falsy) 가 아닌지 확인할 때**
        
        ```jsx
        var elem = null;
        var value = elem && elem.value; // elenem이 falsy 하면 elem이 되는거지 (null)
        ```
        
- 대체
    - `?.` 옵셔널 체이닝 연산자

### `?.` 옵셔널 체이닝 연산자

- **쉽게 말해, 참조시 `&&` 단축 평가의 더 안전 버전**
- **기능**
    - 좌항이 null 또는 undefined ⇒  undefined를 반환
    - 아니면 ⇒ 좌항을 참조
- **쓰임새**
    - **객체를 가리키기를 기대하는 변수가 null 또는 undefined가 아닌지 확인할 때**
        - 바로 위의 `&&` 단축평가는 null, undefined 외에 falsy 한 값이 더 있기 때문에 오류 가능성 O
            
            ⇒  `?.`로 해결!

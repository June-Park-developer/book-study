## 4.1 변수

- **메모리**: 메모리 셀의 집합체, 메모리 셀은 고유의 메모리 주소를 가짐
    - 자바스크립트는 개발자가 직접 메모리 제어 하는 것을 허용  X
- **변수**: 값의 위치를 가리키는 상징적인 이름
    - 하나의 값을 저장하기 위해 확보한 메모리 공간 자체
    - 또는 그 메모리 공간을 식별하기 위해 붙인 이름(식별자)

<aside>

**이해한 바**

- 변수는 메모리 공간을 가리키는 이름
    - 기본형을 담을 때는 실제 값 자체를 담고 있음
    - 참조형을 담을 때는 메모리 주소(참조값)를 담고 있음
</aside>

- **변수명**에 **변수값**을 **할당(대입)**한다

---

## 4.2 식별자

- **식별자**: 어떤 값을 구별해서 식별할 수 있는 고유한 이름 (변수명, 함수명, 클래스명 등)
    - 식별자는 값이 아니라 메모리 주소를 기억하고 있음
    - 네이밍 규칙을 준수해야 함
    - 선언에 의해 자바스크립트 엔진에 식별자의 존재를 알림

---

## 4.3 변수 선언

- **변수 선언**: 변수를 생성하는 것
    - 값을 저장하기 위한 메모리 공간 확보
    - 변수 이름과 확보된 메모리 공간의 주소를 연결
- **선언 과정**
    - **1. 선언** : 변수 이름을 등록해서 자바스크립트 엔진에게 변수의 존재를 알림
    - **2. 초기화** : 메모리 공간을 확보 + 암묵적으로 undefined를 할당해 초기화
- **키워드**
    - var
        - 선언 + 초기화가 동시에
    - let
    - const
- **ReferenceError**
    - 선언하지 않은 식별자에 접근하는 경우 발생하는 에러

---

## 4.4 변수 선언의 실행 시점과 호이스팅

- **선언은 미리 실행됨**
    - 변수 선언이 런타임이 아니라 그 이전 단계에서 미리 실행됨
    - 실행 전에 소스코드의 평가 과정을 거치는데, 그때 선언문을 먼저 찾아서 실행해버림
    - 선언만! 할당은 런타임에 진행됨
- **변수 호이스팅**
    - 이처럼 변수 선언문이 코드의 선두로 끌어올려진 것 처럼 동작
    - 모든 식별자 (var, let, const, function, class 키워드 이용하는 모두) 가 다 호이스팅 됨

---

## 4.5 할당

- **할당 (대입, 저장)**
    - 좌변의 변수 = 우변의 값
    - `=` : 할당 연산자
- **할당 시 새로운 메모리 공간을 이용**
    - undefined로 초기화 된 기존 메모리 공간을 덮어쓰는 게 아님
    - 새로운 메모리 공간을 확보하고 그곳에 할당된 값을 저장한다!

---

## 4.6 재할당

- **재할당** : 이미 값이 할당된 변수에 새로운 값을 또다시 할당
    - 변수는 이름 자체가 변하는 수, 즉 재할당이 가능함
    - 상수 (`const`)는 재할당이 불가능함. 즉 한번만 할당 가능
- **재할당 시 마찬가지로 새로운 메모리 공간을 이용**
    - 이전 값이 들어있던 기존 메모리 공간을 덮어쓰는게 아님
    - 새로운 메모리 공간을 확보하고 그곳에 재할당된 새로운 값을 저장한다!

<aside>

**그럼 기존의 메모리 공간은?**

- 어떠한 식별자도 참조하고 있지 않은 메모리 공간이 된다
- 그럼 언젠가 가비지콜렉터가 정리해줌
</aside>

---

## 4.7 식별자 네이밍 규칙

- **네이밍 규칙** (필수)
    - 특수문자를 제외한 문자, 숫자, `_`, `$` 만 사용 가능
    - 숫자로 시작할 수는 없음
    - 예약어는 사용 불가능
- **네이밍 컨벤션** (권장)
    - camelCase ⇒ 변수명, 함수명
    - PascalCase ⇒ 생성자 함수명, 클래스명
    - snake_case
    - typeHungarianCase

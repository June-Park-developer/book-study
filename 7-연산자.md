## 개요

- **연산자 표현식 : 피연산자(명사) + 연산자(동사)** ⇒ 자체도 하나의 표현식이다~
    - 피연산자: 연산의 대상 (표현식)
    - 연산자 : 하나 이상의 표현식을 연산해서 하나의 새로운 값을 생성함

---

## 7.1 산술 연산자

- 산술 연산자 : 수학적 계산을 수행해 새로운 숫자 값을 만듦

### 7.1.1 이항 산술 연산자

- 2개의 피연산자를 연산함
    
    
    | 연산자 | 의미 | 부수효과 |
    | --- | --- | --- |
    | **+** |  | X |
    | **-** |  | X |
    | ***** |  | X |
    | **/** |  | X |
    | **%** | 나머지 | X |

### 7.1.2 단항 산술 연산자

- 1개의 피연산자를 연산함
    
    
    | 연산자 | 의미 | 부수효과 |
    | --- | --- | --- |
    | **++** |  | O (암묵적 할당) |
    | **—** |  | O (암묵적 할당) |
    | **+** | 아무 효과 없음!
    숫자 아닌 피연산자를 숫자로 변환하여 반환 
    (피연산자 자체를 변경하지 않고 새롭게 생성하는 것) | X |
    | **-** | 부호 반전
    숫자 아닌 피연산자를 숫자로 변환하여 반환 
    (피연산자 자체를 변경하지 않고 새롭게 생성하는 것) | X |
- ++, —
    - 전위 (피연산자 앞)
        - 먼저 증가시켜 암묵적 할당하고, 다른 연산 진행
    - 후위 (피연산자 뒤)
        - 다른 연산 먼저 하고, 다음에 증가시켜서 암묵적 할당

### 7.1.3 문자열 연결 연산자

- +
    - 하나라도 문자열이면 문자열 연결 연산자로 동작
    - 이외에는 다 더하기 (숫자 계산)
        
        ```jsx
        1 + false; // 1 + 0 = 1 
        // 이렇게 문자열 아니면 최대한 숫자로 암묵적 타입 변환 해줌
        ```
        

---

## 7.2 할당 연산자

- 할당 연산자
    - 우항의 값을 좌항의 변수에 할당
    - 변수가 바뀌므로 부수효과 O
    
    | 연산자 | 동일 표현 | 부수효과 |
    | --- | --- | --- |
    | **=** |  | O |
    | **+=** | x = x + 5 | O |
    | **-=** | x = x - 5 | O |
    | ***=** | x = x * 5 | O |
    | **/=** | x = x / 5 | O |
    | **%=** | x = x * 5 | O |
- 할당문은 표현식인 문
    
    ```jsx
    var x;
    console.log(x = 10); // 가능함! 10이라는 값을 표현하는 거지
    ```
    
    - 따라서 여러 변수에 동일한 값을 연쇄할당 가능
        
        ```jsx
        a = b = c = 0; // 오른쪽부터 할당 시작해서 값으로 평가되니까!
        ```
        

---

## 7.3 비교 연산자

- 비교 연산자
    - 좌항과 우항을 비교하고, 그 결과를 불리언 으로 반환
    - 제어문의 조건식으로 주로 사용

### 7.3.1 동등/일치 비교 연산자

- **동등 비교** : 값만 비교
- **일치 비교**: 값 & 타입 비교 (더 엄격함) ⇒ 권장!
    - 주의 1: NaN은 자신과 일치하지 않음!
        
        `NaN === NaN;`  ⇒ false
        
        해결방안:  `Number.isNaN` 함수를 사용해서 NaN인지 확인할 수 있음..또잉
        
    - 주의 2: +0 과 -0 은 일치해버림!
        
        `+0 === -0` ⇒ true
        
        해결방안:  [`Object.is`](http://Object.is) 메서드를 사용하면 주의 1, 2 둘다 제대로 알려준다
        

### 7.3.2 대소 관계 비교 연산자

- `>` , `<`, `<=`, `>=`
- 크기를 비교해서 불리언 값을 반환
- 부수효과 X

---

## 7.4 삼항 조건 연산자

- 조건식의 평가 결과에 따라 반환할 값을 결정 ⇒ 조건문!
- 유일한 삼항 연산자
- **기본 형태**
    
    ```jsx
    조건식 ? 조건식이 true일 때 반환할 값 : 조건식이 false일 때 반환할 값
    ```
    
    - 조건식이 불리언이 아니라면 암묵적으로 타입 변환됨
- **`if else` 와 비교**
    - 같은 조건 문이므로, 대체 가능한 경우가 많음
    - 삼항 조건 연산자는 값으로 평가할 수 있는 표현식인 문!!
        - 따라서 다른 표현식의 일부가 될 수 있어 편함! (그 결과를 다른 변수에 대입한다든가..)
    - 사용
        - 조건에 따라 어떤 값을 결정해야 한다면 ⇒ 삼항 조건 연산자
        - 조건에 따라 수행할 문이 여러줄이라면 ⇒ if else

---

## 7.5 논리 연산자

- `||` (논리합), `&&` (논리곱), `!` (부정)
- 피연산자가 불리언 값이 아니면 불리언으로 암묵적 타입 변환됨
- 결과값
    - `!` : 결과값은 항상 불리언
    - `||`, `&&` : 2개의 피연산자 중 어느 한쪽 → 단축 평가
- 부수효과 없음
- 복잡하면 드 모르간 써서 잘 보이도록 바꿔줘

---

## 7.6 쉼표 연산자 → 새롭고 안 익숙함

- 왼쪽 부터 평가해서 마지막 피연산자의 평가 결과를 반환
    
    ```jsx
    var x, y, z;
    x = 1, y = 2, z = 3; // 3
    ```
    

---

## 7.7 그룹 연산자

- `()`를 통해 우선순위를 가장 높여줌

---

## 7.8 typeof 연산자

- 피연산자의 데이터 타입을 문자열로 반환
- 결과 (총 7가지)
    - “string”, “number”, ‘boolean”, “undefined”, “symbol”, “object”, “function”
- 주의
    - null은 결과가 “object”
        - 값이 null 타입인지 확인하려면 typeof 말고 `===` 쓰도록
    - 선언하지 않은 식별자는 결과가 “undefined”
        - 오류가 안 나니까 주의

---

## 7.9 지수 연산자

- `**`
- 이전에는 `Math.pow(2,2)` 처럼 썼음
- `**=` 도 가능
- 이항 연산자 중에는 우선순위 가장 높음 (수학이랑 똑같이)

---

## 7.10 그 외의 연산자

- 뒤에서 배워요~

---

## 7.11 부수효과

- 다른 코드에 영향을 주는가? (피연산자를 바꾼다든가)
- 부수효과 있는 연산자
    - 할당 연산자
    - 증감 연산자 (`++`, `--`)
    - delete 연산자 (객체의 property 삭제)

---

## 7.12 우선순위

- 상식적인 순서랑 큰 차이 없음! (수학적으로 특히나)
- 헷갈릴만 할 땐 `()`를 쓰도록

---

## 7.13 연산자 결합 순서

- 내가 이해하기로는 우선순위가 같을 때, 어느 쪽 부터 평가하는지
    - 예) `x = y = 3` 이면 `x = (y = 3)` 이므로 대입 연산자는 우→좌 임
- 이정도만 기억하자!
    - **좌 → 우** : 산술 연산자, 비교 연산자, 논리 연산자
    - **우 → 좌** : 대입 연산자, 증감 연산자

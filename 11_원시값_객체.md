## **결론**
    - 원시값은 변경 불가능 ↔ 객체는 변경 가능
    - 원시값은 실제 값 저장 ↔ 객체는 참조 값 저장
    - 원시값은 값에 의한 전달 ↔ 객체는 참조에 의한 전달

---

## 11.1 원시값

### 11.1.1 변경 불가능한 값

- **변수 ≠ 값**
    - 변수는 당연히 변할 수 있는 것 ≠ 원시값은 변할 수 없는 것
    - 상수값은 재할당이 금지된 “변수”임 ≠ 원시값
- **원시값의 불변성**
    - 데이터의 신뢰성 보장
- **변수의 재할당**
    - 새로운 메모리 공간을 확보하고 새로운 값 저장 후 변수가 가리키는 방향을 바꿈
    - 이전의 원시값을 변경하는 것 X

### 11.1.2 문자열과 불변성

- **아닌 것 같지만 원시값이다!**
    - 문자가 몇개냐에 따라 확보해야 할 메모리 공간이 다름
    - BUT 자바스크립트에서는 여전히 원시값 ⇒ 불변성
- **유사 배열 객체**
    - 배열처럼 인덱스로 각 문자에 접근 가능 (변경은 에러 없이 불가능)
    - length 프로퍼티를 갖는 객체
    - for문 순회도 가능
- **이터러블**

### 11.1.3 값에 의한 전달

- **값에 의한 전달**
    - 변수에 원시값을 갖는 변수를 할당하면 ⇒ **원시값이 복사되어 전달**
    - 같은 값을 갖게 되지만, 실제로는 다른 메모리 공간의 **별개의 값**
    - 띠리서 한쪽에서 변경해도 다른 쪽에 **영향을 주지 않음**
- **정확히 말하면 값이 아니라 메모리 주소를 전달**
    - JS에서 식별자는 메모리 주소에 붙인 이름이니까
    - 하지만 결국 식별자로 값에 접근하니까 실제 값을 담고 있는 것과 마찬가지!

---

## 11.2 객체

- **객체**
    - 확보해야 할 메모리 크기를 미리 알 수 없음
    - 프로퍼티 키를 인덱스로 사용하는 해시 테이블이라고 생각하면 됨
        - 일단 내가 이해한 바 : 결국 변수들의 집합이죠? 배열이랑 비슷한 느낌인데 그 변수들의 종류가 다양하고, 인덱스로 문자열 키를 임의로 쓸 수 있는거지. 그러니까 배열도 객체지~!
    - JS는 클래스 없이 객체 생성 가능하며 생성 이후에도 동적으로 프로퍼티 추가 가능

### 11.2.1 변경 가능한 값

- **참조값**
    - 객체를 할당한 변수가 기억하는 메모리 주소를 통해 접근하면 참조값 (주소)가 들어있음
    - 실제 값이 아니라, 그 값이 살고있는 집 주소가 있는거지 (간접적)
    - 따라서 변수는 객체를 “참조하고 있다”, “가리키고 있다” 라고 함
- **변경 가능한 값**
    - 객체를 할당한 변수는 **재할당 없이 객체를 직접 변경할 수 있음!**
    - **장점**
        - 엄청 큰 객체를 수정할 때마다 새로 만들어야 하는 비효율성 극복
    - **단점**
        - 여러 개의 식별자가 하나의 객체를 공유할 수 있음 (주소값을 다들 갖고 있으면 됨)
        - 데이터의 신뢰성이 떨어짐

### 11.2.2 참조에 의한 전달

- **참조에 의한 전달**
    - 객체를 가리키는 변수를 다른 변수에 할당하면 **참조값(주소)가 복사되어 전달됨**
    - 결국 두개의 식별자(변수)가 **하나의 객체를 공유**함
    - 한쪽에서 바꾸면 **서로 영향을 받음**
- **정확히 말하면**
    - 결국 주소값이라는 값을 복사해서 전달함
    - 정확히 말하면 진정한 참조에 의한 전달은 없음 ⇒ 공유에 의한 전달
    - 포인터의 개념이 없으니까~

## 5.1 값

- **값**: 표현식이 평가(해석 or 참조)되어 생성된 결과
    - 모든 값은 데이터 타입을 가짐
    - 변수에 할당되는 것은 값

---

## 5.2 리터럴

- **리터럴**
    - 사람이 이해할 수 있는 문자 또는 약속된 기호를 사용해 값을 생성하는 표기법
    - 값을 생성하기 위해 미리 약속한 표기법
    - 즉, 이렇게 적으면 이러한 값을 만들어줄게~ 하고 정해져 있는 방법들
    - 대표적 예시
        - 정수 리터럴 : 100, 2 …
        - 문자열 리터럴: “Hello”, “World”…
        - 객체 리터럴: { name: “홍길동”, age: 24}

---

## 5.3 표현식

- **표현식**
    - 값으로 평가될 수 있는 문은 모두 표현식
    - 표현식과 그 평가된 값은 동치
    - 따라서 그 자리에 값을 넣어도 이상하지 않으면 표현식이다!
    - 표현식은 다른 표현식의 일부가 되어 새로운 값을 만들어낼 수 있음 (값으로 쓰이니까)
        - 예) const x = 3;   x + 3
- **종류**
    - 리터럴 표현식 : 10, “Hello”
    - 식별자 표현식 (선언이 이미 있다고 가정) : sum, person.name
    - 연산자 표현식 : 10 + 20
    - 함수 호출 표현식 ****(선언이 이미 있다고 가정) : squre()
    

---

## 5.4 문

- **문**
    - 프로그램을 구성하는 기본 단위이자 최소 실행 단위
    - 여러 토큰으로 구성됨
        - 토큰: 문법적인 의미를 가지며, 문법적으로 더이상 나눌 수 없는 코드의 기본 요소
            - 예) `var` `x` `=` `3` `;` 각각이 다 토큰
    - 문은 명령문이라고도 함 ⇒ 문이 실행되면 무슨 일인가가 일어남
- **종류**
    - 선언문
    - 할당문
    - 조건문
    - 반복문

---

## 5.6 표현식인 문과 표현식이 아닌 문

- **문 = 표현식인 문 + 표현식이 아닌 문**

- **구분하는 방법**
    - 변수에 할당할 수 있으면 표현식이다!
        - 변수에 할당할 수 없는 예 (선언문)
            
            ```jsx
            var x; // 선언문
            var foo = var x; // 말이 안되죠?? 즉 선언문은 표현식이 아님
            ```
            
        - 변수에 할당할 수 있는 예 (할당문)
            
            ```jsx
            x = 100; // 할당문
            var foo = x = 100; // 말이 되죠? 즉 할당문은 표현식임
            ```
            

<aside>

### 내가 이해한 바

- **Statement**는 "행동", **Expression**은 "값"
    - Statement는 실행하면 무언가가 일어남, `;` 가 끝에 있음
    - Expression 은 어떤 값을 나타냄
- **둘은 포함관계가 아니라 겹치는 관계**
    - Statement  = 표현식인 문 + 표현식이 아닌 문
        - 표현식인 문 : `x=30;` 할당문
        - 표현식이 아닌 문: `const x;` 선언문
    - Expression =  문인 표현식 + 문이 아닌 표현식
        - 문인 표현식: `x=30;` 할당문
        - 문이 아닌 표현식: `30` 리터럴 표현식
</aside>

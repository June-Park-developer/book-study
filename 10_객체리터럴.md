## 10.1 객체란

- 자바스크립트는 **객체 기반**의 프로그래밍 언어로 거의 모든것이 객체다
- 원시 값을 빼면 **전부다 객체다!**
    - 원시값 : 변경 불가능  ↔ 객체 : 변경 가능
- **객체의 구성**
    
    ```jsx
    var person = {
    	name: "Lee",  // 프로퍼티 = 키 : 값
    	age: 20       // 프로퍼티 = 키 : 값
    };
    ```
    
    - 0개 이상의 프로퍼티
        - 프로퍼티 = (키: 값)
    - 모든 값은 프로퍼티 값이 될 수 있음
        - 함수도 일급 객체이므로 값이다 → 함수도 프로퍼티로 사용할 수 있음 (메서드)
    - **즉, 객체는 프로퍼티와 메서드로 구성된 집합체**!
        - 프로퍼티: 객체의 **상태**를 나타내는 값
        - 메서드: 프로퍼티를 참조하고 조작할 수 있는 **동작**
            - 정확히 말하면 메서드가 프로퍼티에 포함되긴 하지만 기능상 이렇게 구분하는 듯
- **객체의 의의**
    - 상태와 동작을 하나의 단위로 구조화 가능
- **객체 지향 프로그래밍**
- **객체와 함수**
    - 함수 →  객체를 생성
    - 함수 자체가 객체

---

## 10.2 객체 리터럴에 의한 객체 생성

- JAVA는 클래스기반 객체지향 언어 → 클래스를 통해 `new` 로만 생성 가능
- JS는 **프로토타입 기반** 객체지향 언어 → 다양하게 생성 가능
    - **객체 리터럴**
    
    — 이외에는 전부 함수로 객체 생성 —
    
    - **Object 생성자 함수**
    - **생성자 함수**
    - **Object.create 메서드**
    - **클래스 (ES6)**

### 객체 리터럴

- 객체를 생성하기 위한 표기법
- **형태**
    
    ```jsx
    { 0개 이상의 프로퍼티 };
    ```
    
    - 주의 : `{}`는 코드 블록이 아니라 값으로 표현되는 표현식! ⇒ `;` 필요

---

## 10.3 프로퍼티

- **프로퍼티 = 키 + 값**

### **키**

- **모든 문자열 (빈 문자열 가능) | 심벌값**
    - 이외의 값은 암묵적 타입변환으로 문자열이 됨
    - 예약어도 가능함 → 권장 X
    - 중복 시 마지막 것이 덮어씀
- **프로퍼티 값에 접근할 수 있는 식별자**
    - 식별자 네이밍 규칙을 따르면 → 따옴표 없어도 됨  ⇒ 가급적 권장!
    - 식별자 네이밍 규칙을 따르지 않으면 → 따옴표 있어야 됨
- **동적 프로퍼티 키**
    - `[]`로 표현식을 묶으면 됨
        
        ```jsx
        var obj = {};
        var key = "hello";
        
        obj[key] = "world"; // 그럼 var obj = {hello: "world"};
        ```
        

### 값

- 모든 값 가능

---

## 10.4 메서드

- 프로퍼티 값이 함수일 경우 메서드라 부름 ⇒ 즉, **메서드는 객체에 묶여있는 함수**

---

## 10.5 프로퍼티 접근

- **프로퍼티 접근 방법 두가지**
    - 마침표 표기법 `.`
    - 대괄호 표기법 `[]`
        - 안에 따옴표로 감싼 문자열 (안그러면 식별자로 해석)
        - 식별자 네이밍 규칙을 준수하지 않는 이름은 대괄호만 가능
- 존재하지 않는 프로퍼티에 접근 시 ⇒ undefined 반환

---

## 10.6 프로퍼티 값 갱신

- 이미 존재하는 프로퍼티에 값을 할당 시 갱신됨
    - 찾아보니 재할당이 아니라 갱신이라고 따로 부르는 이유가 있음
    - 프로퍼티 값이 원시값이더라도, 이는 새로운 메모리를 파는게 아니고 값만 바뀌는 진짜 갱신이네

---

## 10.7 프로퍼티 동적 생성

- 존재하지 않는 프로퍼티에 값을 할당 ⇒ 동적으로 프로퍼티가 생성+할당됨

---

## 10.8 프로퍼티 삭제

- **`delete` 연산자**
    - 객체의 프로퍼티 삭제
    - 예: `delete person.name;`
- 주의: 존재하지 않는 프로퍼티 삭제 시 에러 없이 그냥 무시됨

---

## 10.9 ES6에서 추가된 확장 기능

### 10.9.1 프로퍼티 축약 표현

- 프로퍼티 값으로 변수를 사용 & 변수 이름이 프로퍼티 키와 같을 시 생략 가능
    
    ```jsx
    const name = "Lee";
    const age = 20;
    
    const obj = {name, age};
    // const obj = {name: name, age: age}; 
    // 아주 많이 썼죠? 
    ```
    

### 10.9.2 계산된 프로퍼티 이름

- 원래 프로퍼티 키를 동적으로 생성할 때  ⇒ `[표현식]` 으로 표현 가능
    
    ```jsx
    var i = 0;
    var obj = {};
    
    obj[++i + '번'] = i;
    obj[++i + '번'] = i;
    
    // obj = {1번: 1, 2번: 2};
    ```
    
- ES6 이후에는 객체 리터럴 내부에서도 `[표현식]`으로 표현 가능
    
    ```jsx
    let i = 0;
    
    const obj = {
    	[++i + `번`]: i,
    	[++i + `번`]: i
    };
    ```
    

### 10.9.3 메서드 축약 표현

- 원래 메서드는 프로퍼티 값에 함수를 할당하는 형태였음
    
    ```jsx
    var obj = {
    	name: "Lee",
    	sayHi: function(){  console.log("Hi"); }
    };
    ```
    
- ES6 이후에는 `function` 생략 가능
    
    ```jsx
    const obj = {
    	name: "Lee",
    	sayHi() { console.log("Hi"); }
    }; 
    ```
    
    - 이 둘은 다르게 동작한다! 나중에 배워보아요
